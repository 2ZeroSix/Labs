Алгоритм Хаффмана
	a_i - символы
	f_i - частота элемента
	A = {a_1, ... , a_n}
	M - сообщение из символьного алфавита
	1
		чтение файла:
			заполнение таблицы f[256] кол-ом встреченных символов

		a_1 ... a_n	--\
		f_1 ... f_n	--/ сортировка по возрастанию(самые редкие впереди)

	2 построение дерева Хаффмана
		помещаем в очередь с приоритетом все вершины деревьев (хранящие по одному символу с кол-ом появлений > 0)

		цикл
			v[1] = pop(Q)
			v[2] = pop(Q)
			W = createNode(v1, v2)
			push(w, Q)
			пока в очереди больше 1 элемента


код Хаффмана - Битовый код символа (путь к символу)
TLV